#include<stdio.h>
main()
{
	float w,l,area;
	printf("enter the value of w:");
	scanf("%f",&w);
		printf("enter the value of l:");
			scanf("%f",&l);
					//area of rectangle

			area=w*l;
			printf("area is %f",area);

}
